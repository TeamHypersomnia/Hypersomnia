#pragma once

namespace augs {
	namespace gui {
		enum class flag {
			DISABLE_HOVERING,
			ENABLE_DRAWING,
			FETCH_WHEEL,
			PRESERVE_FOCUS,
			FOCUSABLE,
			ENABLE_DRAWING_OF_CHILDREN,
			SNAP_SCROLL_TO_CONTENT_SIZE,
			SCROLLABLE,
			CLIP,

			COUNT
		};
	}
}
