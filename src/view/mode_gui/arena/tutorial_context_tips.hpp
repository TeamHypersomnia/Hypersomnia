#pragma once

template <class F, class H>
void do_tutorial_context_tip(uint32_t index, F text, L break_line, H hotkey) {
	switch (index) {
		case 0:
		text("Welcome to Hypersomnia!"


		default:
		break;
	}
}

