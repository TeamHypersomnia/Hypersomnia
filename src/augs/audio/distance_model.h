#pragma once

namespace augs {
	enum class distance_model {
		// GEN INTROSPECTOR enum class augs::distance_model
		NONE,
		INVERSE_DISTANCE,
		INVERSE_DISTANCE_CLAMPED,
		LINEAR_DISTANCE,
		LINEAR_DISTANCE_CLAMPED,
		EXPONENT_DISTANCE,
		EXPONENT_DISTANCE_CLAMPED
		// END GEN INTROSPECTOR
	};
}
