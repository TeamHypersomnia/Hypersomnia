#pragma once

namespace augs {
	struct snapshotted_player_settings {
		// GEN INTROSPECTOR struct augs::snapshotted_player_settings
		unsigned snapshot_interval_in_steps = 800;
		// END GEN INTROSPECTOR
	};
}
