#pragma once
#include "game/container_sizes.h"

namespace assets {
	enum class font_id {
		// GEN INTROSPECTOR enum class assets::font_id
		INVALID,
		GUI_FONT,
		REQUISITE_COUNT,
		COUNT = MAX_FONT_COUNT + 1
		// END GEN INTROSPECTOR
	};
}