#pragma once

namespace assets {
	enum class game_image_id;
	enum class sound_buffer_id;
	enum class particle_effect_id;

	enum class game_image_id;
	enum class sound_buffer_id;
	enum class particle_effect_id;
}
