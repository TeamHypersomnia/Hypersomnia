#pragma once

namespace assets {
	enum class image_id;
	enum class sound_buffer_id;
	enum class particle_effect_id;

	enum class image_id;
	enum class sound_buffer_id;
	enum class particle_effect_id;
}
