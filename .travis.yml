dist: trusty
sudo: required
language:
  - cpp
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-7
      - g++-7
      - cmake
python:
  - "3.6"
script:
  # Check versions
  - gcc -v && g++ -v && cmake --version && make --version
  - python --version
  # Run your build commands next
  - mkdir dev
  - cd dev
  - cmake -DUNIX=ON -DBUILD_HTTP_REQUESTS=OFF -DADD_COMMAND_TO_ARCHIVE_BUILDS_IN_RELEASE=OFF -D CMAKE_C_COMPILER=gcc-7 -D CMAKE_CXX_COMPILER=g++-7 ..
  - make --keep-going
  - ./Hypersomnia --unit-tests-only
  
notfications:
  email: false
